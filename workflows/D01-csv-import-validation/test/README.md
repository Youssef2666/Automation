# D01 test inputs

- `customers-sample.csv` - 8 rows, 4 of them broken (bad e-mail, missing name, duplicate `SMP-0001`, unknown
  segment `gold`). Expected: 4 imported, 4 rejected.
- `xlsx-variant.json` - generated by the authoring script: the same workflow with `Config` preset to
  `seed/files/customers.xlsx` (id `ALD01XlsxVariant`). Import it and run `python scripts/dev/run-workflow.py ALD01XlsxVariant`.

To run the sample through the real workflow: copy it into the inbox mount and point `Config.file` at it
(`/home/node/.n8n-files/data/inbox/customers-sample.csv`), or edit the authoring script's default and regenerate.

```bash
cp workflows/D01-csv-import-validation/test/customers-sample.csv data/inbox/
```

Default run (seed file, 60 rows): `python scripts/dev/run-workflow.py D01` -> 57 imported, 3 rejected
(rows 13, 28, 52), `data/out/rejects-<timestamp>.csv`, one e-mail in Mailpit with the CSV attached.
