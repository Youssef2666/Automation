# R04 test inputs

- Input PDF: `seed/files/invoice-locked.pdf` (text-based, one line-item table; generated by `seed/generate_seed.py`).
- `expected.json` - the record the "Parse fields" node produces for it (header fields, 6 items, checks all true).

```bash
curl -s -X POST localhost:8090/pdf/extract -F "file=@seed/files/invoice-locked.pdf" | python -m json.tool | head -40
python scripts/dev/run-workflow.py R04
```
