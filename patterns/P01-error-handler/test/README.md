# P01 test fixtures

- `input.json` - the item the Error Trigger emits when a node fails (shape from n8n 2.37); feed it to the
  "Normalize error" Code node to check the mapping in `expected.json`.
- `failing-caller.json` - a webhook workflow (`POST /webhook/p01-fail`) whose only node always fails and whose
  `settings.errorWorkflow` points at P01. Generated by the P01 authoring script. Import, publish, curl it: P01 runs
  for real (manual/CLI runs never trigger error workflows).
